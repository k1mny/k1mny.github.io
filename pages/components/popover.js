export default function PopoverInfo({ isOpen, title, text }) {
  return (
    <div
      style={{
        position: 'absolute',
        width: '300px',
        height: '200px',
        background: 'rgba(255,255,255,0.5)',
        zIndex: '10',
      }}
    >
      test
    </div>
  );
}
