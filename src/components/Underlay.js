export default function Underlay() {
  return (
    <div
      style={{
        position: 'absolute',
        top: 0,
        left: 0,
        width: '100%',
        height: '100%',
        fontSize: '20px',
        padding: 40,
        display: 'inline-flex',
        flexDirection: 'column',
        alignItems: 'start',
        justifyContent: 'start',
        pointerEvents: 'none',
      }}
    >
      kimny
    </div>
  );
}
